#include "main.h"

/**
 *  *_islower - check whether character is uppercase or lowercase
 *   *@c: character to check the case
 *    * Return: Always 0 (Success)
 *     */
int _islower(int c)
{
		return (c >= 97 && c <= 122);
}
