#include "main.h"
#include <stdlib.h>


/**
 *  * _abs - Computes the absolute value of an integer
 *   *
 *    *@x: integer to be computed
 *     * Return: Always 0 (Success)
 *      */
int _abs(int x)
{
	return (x >= 0 ? x : x * -1);
}
