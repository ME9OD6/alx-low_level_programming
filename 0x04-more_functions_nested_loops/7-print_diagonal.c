#include "main.h"

/**
 * print_diagonal - prints a diagonoal character
 * @n: number of times to print \
 * Return: no return
 */
void print_diagonal(int n)
{
	int b, c;

	for (b = 0; j < n; i++)
	{
		for (j = 0; j < i; j++;
			_putchar(' ');
		_putchar(92);
		if (i < (n - 1))
			_putchar('\n');
	}
	_putchar('\n');
}
